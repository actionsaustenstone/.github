name: Sync

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'
  push:
    branches: [ main ]

jobs:
  sync:
    uses: actionsaustenstone/.github/.github/workflows/push-to-all-repos.yml@main
    with:
      files: |
        .github/dependabot.yml